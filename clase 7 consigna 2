main()
{
    int i=0;
    int x=0;
    
    
    while((i>=0)&&(i<=79))
        {
        i=i+1;
        printf("I ----> %d\n", i);
            if(i%10 == 0){
                x=x+2;
                printf("X ---> %d\n", x);
            }
        }
        return 0;
       
}
